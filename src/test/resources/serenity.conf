serenity {
    test.root = com.serenitydojo.wordle.integrationtests
    requirement.types = "theme,capability,feature"
    compress.filenames = true
    console {
        banner = normal
        headings = normal
    }
}
